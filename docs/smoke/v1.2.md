# v1.2 Mass Smoke Test

Every test for every milestone in one place. Tick as you go. Pre-ticked items were verified during the autonomous build session (see Iniaki's confirmations in chat).

---

## M0 — v1.1 Closure

- [x] Creator: `/creator/inbox` shows breadcrumb **My campaigns › Submission inbox**; link works.
- [x] Clipper: `/submissions` shows breadcrumb **Campaigns › My submissions**; link works.
- [x] Step 4 of campaign creation: **TikTok + Instagram only** (no YouTube).
- [x] Hub filter sheet: **Platform** options show only TikTok + Instagram.
- [x] Login page hero + features mention "TikTok and Instagram Reels" — no YouTube.
- [ ] **OAuth scope-upgrade resilience:** publish a per-sub campaign as a creator without `write:tracking_links` → bounce through Fanvue OAuth → land back on `/creator/campaigns/{draftId}/edit` (not `/creator/campaigns`). *(Iniaki confirmed the redirect fires; full round-trip not yet exercised because dev backend wasn't running.)*

---

## M1 — Subscriber Campaign IA

- [x] **Mode modal:** new campaign → modal pops with two cards (Clip / Subscriber + best-for hints); pick Subscriber → modal closes.
- [x] **Modal skips on edit:** edit existing draft → modal does NOT pop.
- [x] **Mode-aware platforms (step 4):** per-view = TikTok + Instagram; per-sub = Instagram, TikTok, Reddit, X.
- [x] **Hub badges:** cards show **Clip** badge (sky) or **Subscriber** badge (violet) top-left.
- [x] **Per-sub card rate:** top-right reads `$X / sub` (not `/ 1K`).
- [x] **Per-sub card hides views row.**
- [x] **Detail page badge:** type badge above the campaign title (size="md").
- [x] **Hub filters:** Campaign type select (All / Clip / Subscriber); Creator trust checkbox ("100% paid track record only"); both reflected in active-filter chip.
- [x] **Login + hub copy:** mention both modes; subscriber-link platforms (Reddit, X) called out.
- [x] **Spend preview (step 5):** per-sub reads "At $X per subscriber, $Y covers approximately Z acquired subscribers." Per-view unchanged.
- [x] **Per-view-only fields hidden on per-sub:** "Min views before submittable" + "Max payout per clip" no longer render on per-sub mode.
- [x] **Step 4 per-sub copy:** header reads "Where promoters can share your tracking link — clip, post, story, or thread" (text-post friendly for X/Reddit).

---

## M2 — Promoter Workspace + Creator Roster

**Pre-req:** DB migration `0006_v1_2_m2_workspace.sql` applied, Render Blueprint re-synced for the 30-min cadence.

- [ ] **Schema applied:** `campaigns.traffic_rules` and `submissions.last_clicks` columns exist in prod.
- [ ] **Cron schedule:** Render shows `sync-subscribers` running every 30 min.
- [ ] **Promoter workspace 3-card layout:** approved per-sub submission shows Clicks / Subs acquired / Earnings (was 2 cards: Subs / Earnings).
- [ ] **Clicks ingestion:** within ~30 min of a real click on a tracking link, the workspace's Clicks counter > 0.
- [ ] **Traffic rules round-trip:** creator types newline-separated rules in step 5 textarea → publish → approved promoter sees the rules block in workspace, newlines preserved.
- [ ] **Anti-bot checkbox:** Apply button disabled until the "I won't drive this campaign with bots…" checkbox is ticked.
- [ ] **Tracking-link naming:** Fanvue dashboard shows new mints as `<campaign> · Clipper N (@handle)` (was `<handle> · <campaign>`).
- [ ] **Manual-mode approval:** creator inbox shows pending per-sub applies; approving mints the tracking link.
- [ ] **Creator roster (budget page):** `/creator/campaigns/{id}/budget` for a per-sub campaign shows the roster with **Clicks** column on each row. Hint copy reads "Sub + click deltas land every 30 min via the attribution cron."
- [ ] **"Manage promoters" CTA:** detail page for a per-sub campaign owner shows a violet-tinted card with "Open promoter roster" button → routes to budget page.
- [ ] **Auto-approve cron untouched:** clip per-views auto-approve at 48h still works.

---

## M3 — Revoke Link UX

- [ ] **Revoke button on Accruing rows:** budget page roster, Accruing section shows Revoke button.
- [ ] **Revoke confirm dialog:** click Revoke → AlertDialog explains link will be deleted, accrued earnings stand → confirm.
- [ ] **Revoke happy path:** row moves to **Revoked** group at bottom; Fanvue dashboard shows the link is gone.
- [ ] **Earnings preserved post-revoke:** revoked submission's `pendingEarnings` value unchanged from the snapshot when it was Accruing.
- [ ] **Cron skips revoked rows:** next sync-subscribers tick logs no update for revoked submissions.
- [ ] **Auto-revoke on Fanvue-side delete:** delete a tracking link directly in Fanvue dashboard → wait 30 min → submission flips to revoked; Render log shows `Auto-revoked submission <id>: tracking link <uuid> not in Fanvue listing`.
- [ ] **Clipper-side status:** revoked submission shows "Link revoked" badge on `/submissions`.
- [ ] **Ban path still works:** existing ban flow (separate button) deletes the Fanvue link AND blacklists the clipper. Status set to `rejected`, not `revoked`.

---

## M2.5 — Sub Price Context (DEFERRED — no smoke needed)

- [ ] N/A — no code shipped. Re-entry blocked on Fanvue API endpoint confirmation. See `docs/decisions/v1.2-m2.5.md`.

---

## M4 — PostHog Analytics

**Pre-req:** PostHog project created, env vars set on Vercel + Render, both redeployed.

- [ ] **Identify on login:** PostHog → Persons → row appears for your `users.id` with `role` and `handle` properties.
- [ ] **`campaign_created`:** publish a draft campaign → event lands with `payout_type`, `total_budget_cents`, `is_private`.
- [ ] **`subscriber_campaign_created`:** publishing per-sub fires this alongside `campaign_created`, with `rate_per_sub_cents`, `application_mode`.
- [ ] **`submission_created`:** apply to a per-sub campaign as a clipper → event with `payout_type`, `application_mode`, `auto_approved`.
- [ ] **`tracking_link_minted`:** auto-mode apply mints the link → event after `submission_created`. Manual-mode apply fires on creator approval.
- [ ] **`tracking_link_copied`:** clipper clicks copy CTA in workspace → event with `submission_id`, `campaign_id`.
- [ ] **`tracking_link_revoked`:** creator revokes → event with `submission_id`, `campaign_id`.
- [ ] **`submission_marked_paid`:** creator marks paid → event with `method`, `amount_cents`.
- [ ] **Reset on logout:** logout → log in as a different user → events attribute to new user, not previous.
- [ ] **Subscriber Campaign Funnel dashboard:** built per `docs/setup/v1.2-m4.md` step 4(a). Funnel shows steps from `subscriber_campaign_created` → `tracking_link_minted` → `tracking_link_copied` → `submission_marked_paid`.
- [ ] **Deferred events accept-and-park:** `payment_confirmed`, `payment_disputed`, `dispute_resolved`, `payout_settings_saved`, `oauth_scope_upgrade_completed`, `subscriber_attribution_synced` are NOT wired in this rollup; queued for a follow-up M4.1.x PR. Smoke is N/A.

---

## Cross-cutting

- [ ] **Anti-botting note in login footer:** sign-in card footer reads "By continuing you agree to Fanvue's Terms of Service, including Content Rewards' anti-botting policy (campaigns may only be promoted with organic traffic)."
- [ ] **(External — Jackson)** Fanvue's main TOS updated with the full anti-botting clause from `docs/setup/v1.2-cross-cutting.md`.
- [ ] **(External — Jackson)** Privacy policy / TOS audit for draft language complete.
- [ ] **(External — Iniaki + Jackson)** Domain decision made; if `rewards.fanvue.com`, DNS + Vercel + Fanvue dev portal updated per setup doc.

---

## Health checks (post-deploy, ongoing)

- [ ] No spike in `Tracking link mint failed` or `Auto-revoked submission` log lines beyond expected per-flow rates.
- [ ] PostHog Live Events shows steady event volume for the funnel.
- [ ] No 500s on `/api/cron/sync-subscribers` after the cadence bump (4× more invocations).
