/**
 * v1 launch is shipping without payments. Wrap every wallet/escrow/balance
 * surface behind this flag instead of deleting components — flip back to true
 * to restore billing without a code archaeology dig.
 */
export const PAYMENTS_V1_ENABLED = false
